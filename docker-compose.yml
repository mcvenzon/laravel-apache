services:
  web:
    image: laravel-apache-app
    build:
      target: web
      context: .
    env_file:
      - .env
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html

volumes:
  dbdata:
